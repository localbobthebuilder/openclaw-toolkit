diff --git a/extensions/ollama/src/stream-runtime.test.ts b/extensions/ollama/src/stream-runtime.test.ts
index 3019c1cf7..eaeeb06a5 100644
--- a/extensions/ollama/src/stream-runtime.test.ts
+++ b/extensions/ollama/src/stream-runtime.test.ts
@@ -81,6 +81,29 @@ describe("buildOllamaChatRequest", () => {
       model: "library/qwen3:32b",
     });
   });
+
+  it("forwards tool choice when provided", () => {
+    expect(
+      buildOllamaChatRequest({
+        modelId: "qwen3.5:9b",
+        messages: [{ role: "user", content: "hello" }],
+        tools: [
+          {
+            type: "function",
+            function: {
+              name: "read",
+              description: "Read a file",
+              parameters: { type: "object" },
+            },
+          },
+        ],
+        toolChoice: "required",
+      }),
+    ).toMatchObject({
+      tools: expect.any(Array),
+      tool_choice: "required",
+    });
+  });
 });
 
 describe("createConfiguredOllamaCompatStreamWrapper", () => {
@@ -265,6 +288,54 @@ describe("createConfiguredOllamaCompatStreamWrapper", () => {
     );
   });
 
+  it("forwards toolChoice on native Ollama chat requests when configured", async () => {
+    await withMockNdjsonFetch(
+      [
+        '{\"model\":\"m\",\"created_at\":\"t\",\"message\":{\"role\":\"assistant\",\"content\":\"\",\"tool_calls\":[{\"function\":{\"name\":\"read\",\"arguments\":{\"path\":\"/tmp/a\"}}}]},\"done\":false}',
+        '{\"model\":\"m\",\"created_at\":\"t\",\"message\":{\"role\":\"assistant\",\"content\":\"\"},\"done\":true,\"prompt_eval_count\":1,\"eval_count\":1}',
+      ],
+      async (fetchMock) => {
+        const baseStreamFn = createOllamaStreamFn("http://ollama-host:11434");
+        const model = {
+          api: "ollama",
+          provider: "ollama",
+          id: "qwen3:32b",
+          contextWindow: 131072,
+        };
+
+        const stream = await Promise.resolve(
+          baseStreamFn(
+            model as never,
+            {
+              messages: [{ role: "user", content: "hello" }],
+              tools: [
+                {
+                  name: "read",
+                  description: "Read a file",
+                  parameters: { type: "object" },
+                },
+              ],
+            } as never,
+            { toolChoice: "required" } as never,
+          ),
+        );
+
+        await collectStreamEvents(stream);
+
+        const requestInit = getGuardedFetchCall(fetchMock).init ?? {};
+        if (typeof requestInit.body !== "string") {
+          throw new Error("Expected string request body");
+        }
+        const requestBody = JSON.parse(requestInit.body) as {
+          tool_choice?: string;
+          tools?: unknown[];
+        };
+        expect(requestBody.tool_choice).toBe("required");
+        expect(requestBody.tools?.length).toBe(1);
+      },
+    );
+  });
+
   it("passes resolved provider request timeouts to native Ollama chat fetches", async () => {
     await withMockNdjsonFetch(
       [
diff --git a/extensions/ollama/src/stream.ts b/extensions/ollama/src/stream.ts
index ec6412877..3a9bb21e0 100644
--- a/extensions/ollama/src/stream.ts
+++ b/extensions/ollama/src/stream.ts
@@ -341,6 +341,7 @@ export function buildOllamaChatRequest(params: {
   providerId?: string;
   messages: OllamaChatMessage[];
   tools?: OllamaTool[];
+  toolChoice?: OllamaToolChoice;
   options?: Record<string, unknown>;
   requestParams?: Record<string, unknown>;
   stream?: boolean;
@@ -350,6 +351,7 @@ export function buildOllamaChatRequest(params: {
     messages: params.messages,
     stream: params.stream ?? true,
     ...(params.tools && params.tools.length > 0 ? { tools: params.tools } : {}),
+    ...(params.toolChoice !== undefined ? { tool_choice: params.toolChoice } : {}),
     ...(params.options ? { options: params.options } : {}),
     ...params.requestParams,
   };
@@ -431,10 +433,23 @@ interface OllamaChatRequest {
   messages: OllamaChatMessage[];
   stream: boolean;
   tools?: OllamaTool[];
+  tool_choice?: OllamaToolChoice;
   options?: Record<string, unknown>;
   think?: OllamaThinkValue;
 }
 
+type OllamaToolChoice =
+  | "auto"
+  | "none"
+  | "any"
+  | "required"
+  | {
+      type: "function";
+      function: {
+        name: string;
+      };
+    };
+
 interface OllamaChatMessage {
   role: "system" | "user" | "assistant" | "tool";
   content: string;
@@ -922,6 +937,7 @@ export function createOllamaStreamFn(
           messages: ollamaMessages,
           stream: true,
           tools: ollamaTools,
+          toolChoice: (options as { toolChoice?: OllamaToolChoice } | undefined)?.toolChoice,
           options: ollamaOptions,
           requestParams: resolveOllamaTopLevelParams(model),
         });
